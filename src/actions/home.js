const addUser = payload => (dispatch, getState) => {
  return { ...payload };
};

const deleteUser = payload => (dispatch, getState) => {
  return { ...payload };
};

export default {
  addUser,
  deleteUser
};
